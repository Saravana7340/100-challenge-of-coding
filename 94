class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        s=' '.join(s.split())
        wd=s.split()
        wd.reverse()
        return ' '.join(wd)
